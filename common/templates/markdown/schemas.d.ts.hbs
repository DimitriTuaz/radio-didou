/** Loopback 4 - OpenApi Schemas **/

{{#unless openapi.__noSchemas}}
{{#each openapi.components.schemas}}
export interface {{this.title}} {
{{#each this.properties}}
  {{@key}}{{#unless (isRequired ../this @key)}}?{{/unless}}: {{this.type}}
{{/each}}
}

{{/each}}
{{/unless}}
